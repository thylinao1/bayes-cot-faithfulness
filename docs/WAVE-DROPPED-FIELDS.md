# Wave dropped-field audit

Every row `bcf/wave.sh` actually submitted, before the `fix/wave-last-field` fix landed, silently lost its own LAST `KEY=VALUE` field from the sbatch `--export=` line (`row_fields()`'s missing trailing newline; DECISION-LOG.md 2026-09-08 09:39/09:45, `docs/HOLDS-LIFT-PLAN.md` Step 0, `tests/test_wave_row_fields.py`). This audits every wave that `~/bcf/feeder-state.json` (schema `bcf.wave_feeder.v1`, updated `2026-09-08T09:49:25+08:00`) or the DECISION-LOG records as submitted, and reports which field each row lost and whether that field was ever load-bearing.

## Denominators

- rows submitted: **42**
- dropped field informational only (never read at runtime): **37**
- dropped field load-bearing (a running job's behavior would differ): **5**
- dropped field unclassified (no runtime read found either way): **0**

## Every submitted row, by wave

| wave | model | substrate | cue | dropped field | verdict | evidence |
|---|---|---|---|---|---|---|
| `a100-40-01.tsv` | Qwen/Qwen3-8B | arc_challenge | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-01.tsv` | allenai/Olmo-3-7B-Think | arc_challenge | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-01.tsv` | deepseek-ai/DeepSeek-R1-0528-Qwen3-8B | arc_challenge | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-01.tsv` | deepseek-ai/DeepSeek-R1-Distill-Llama-8B | arc_challenge | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-01.tsv` | google/gemma-2-9b-it | arc_challenge | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-01.tsv` | meta-llama/Llama-3.1-8B-Instruct | arc_challenge | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-01.tsv` | microsoft/Phi-4-reasoning | arc_challenge | stated-hint | `BCF_EXTRA_VLLM` | load-bearing | read at runtime in bcf/serve_and_run.sbatch (non-comment line) |
| `a100-40-01.tsv` | openai/gpt-oss-20b | arc_challenge | stated-hint | `BCF_QUANT` | load-bearing | read at runtime in bcf/serve_and_run.sbatch (non-comment line) |
| `a100-40-02.tsv` | Qwen/Qwen3-8B | arc_challenge | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-02.tsv` | google/gemma-2-9b-it | arc_challenge | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-02.tsv` | meta-llama/Llama-3.1-8B-Instruct | arc_challenge | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-03.tsv` | Qwen/Qwen3-8B | arc_challenge | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-03.tsv` | google/gemma-2-9b-it | arc_challenge | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-03.tsv` | meta-llama/Llama-3.1-8B-Instruct | arc_challenge | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-04.tsv` | Qwen/Qwen3-8B | arc_challenge | grader-code | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-04.tsv` | google/gemma-2-9b-it | arc_challenge | grader-code | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-04.tsv` | meta-llama/Llama-3.1-8B-Instruct | arc_challenge | grader-code | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-05.tsv` | Qwen/Qwen3-8B | aqua_rat | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-05.tsv` | google/gemma-2-9b-it | aqua_rat | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-05.tsv` | meta-llama/Llama-3.1-8B-Instruct | aqua_rat | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-06.tsv` | Qwen/Qwen3-8B | aqua_rat | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-06.tsv` | google/gemma-2-9b-it | aqua_rat | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-06.tsv` | meta-llama/Llama-3.1-8B-Instruct | aqua_rat | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-07.tsv` | Qwen/Qwen3-8B | aqua_rat | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-07.tsv` | allenai/Olmo-3-7B-Think | aqua_rat | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-07.tsv` | deepseek-ai/DeepSeek-R1-0528-Qwen3-8B | aqua_rat | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-07.tsv` | deepseek-ai/DeepSeek-R1-Distill-Llama-8B | aqua_rat | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-07.tsv` | google/gemma-2-9b-it | aqua_rat | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-07.tsv` | meta-llama/Llama-3.1-8B-Instruct | aqua_rat | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-07.tsv` | microsoft/Phi-4-reasoning | aqua_rat | metadata | `BCF_EXTRA_VLLM` | load-bearing | read at runtime in bcf/serve_and_run.sbatch (non-comment line) |
| `a100-40-07.tsv` | openai/gpt-oss-20b | aqua_rat | metadata | `BCF_QUANT` | load-bearing | read at runtime in bcf/serve_and_run.sbatch (non-comment line) |
| `a100-40-08.tsv` | Qwen/Qwen3-8B | aqua_rat | grader-code | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-08.tsv` | google/gemma-2-9b-it | aqua_rat | grader-code | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-08.tsv` | meta-llama/Llama-3.1-8B-Instruct | aqua_rat | grader-code | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-09.tsv` | Qwen/Qwen3-8B | logiqa2 | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-09.tsv` | google/gemma-2-9b-it | logiqa2 | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-09.tsv` | meta-llama/Llama-3.1-8B-Instruct | logiqa2 | stated-hint | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-resub-01.tsv` | google/gemma-2-9b-it | arc_challenge | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-resub-01.tsv` | meta-llama/Llama-3.1-8B-Instruct | arc_challenge | professor | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-resub-01.tsv` | Qwen/Qwen3-8B | arc_challenge | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `a100-40-resub-01.tsv` | google/gemma-2-9b-it | arc_challenge | metadata | `BCF_EXPECTED_HOURS` | informational | verified repo-wide: every occurrence outside wave manifests themselves is either bcf/wave.sh's own planning-time MAX_HOURS prescan (line ~286, feeds only a --resume-legs NOTE, never exported to a job) or a manifest-writing call site (bcf/plan_waves.py, bcf/plan_enrich_waves.py, src/bayes_cot_faithfulness/ladder/serve_manifest.py); no submitted job reads it |
| `explore-phi4-off-01.tsv` | microsoft/Phi-4-reasoning | arc_challenge | stated-hint | `BCF_OUT_SUBROOT` | load-bearing | read at runtime in bcf/serve_and_run.sbatch (non-comment line) |

## Reading this table

- **informational**: the dropped field was recorded in the manifest but never read by a submitted job (e.g. `BCF_EXPECTED_HOURS`, planning-time metadata only). These rows ran under the configuration their manifest names; the field loss changed nothing about what actually executed.
- **load-bearing**: a submitted job's behavior, output location, or serving configuration would have silently differed from what the manifest row names, with no error anywhere in the pipeline to catch it. `BCF_OUT_SUBROOT` (job 828564) is the one row in this audit where that already happened for real, caught and cancelled before any arms recorded; every other load-bearing row listed here is a **near miss** -- the field it lost never happened to be the true last field before now, or the row has not yet been re-verified against the fixed `bcf/wave.sh`.
- **unknown**: no runtime read was found for the dropped field in `bcf/serve_and_run.sbatch` or `bcf/env.sh`. Treated as load-bearing for the denominator above until someone confirms otherwise; a static grep proves a read exists, not that one doesn't.

All of these rows were submitted before the fix landed; none has been re-submitted or re-verified against the fixed `bcf/wave.sh` by this script (it only reads manifests and feeder state, never sbatch/squeue). Cells whose dropped field was load-bearing are candidates for the resubmission the orchestrator already has queued (`docs/HOLDS-LIFT-PLAN.md` Step 6's `a100-40-resub-02.tsv`), not evidence that anything published is currently wrong -- read each row's own result files before drawing that conclusion.
